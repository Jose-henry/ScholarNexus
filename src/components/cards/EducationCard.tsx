"use client"

import {Avatar} from 
"@nextui-org/avatar";
import Image from "next/image";

interface Props {
    programme: string;
    school: string;
    level: string;
}

export default function EducationCard({programme, school, level}: Props) {
    return (
        <div> 
            
        </div>
    );
}