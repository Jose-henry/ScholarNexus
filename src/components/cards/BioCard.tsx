"use client"

import {Avatar} from 
"@nextui-org/avatar";
import Image from "next/image";

interface Props {
    bio: string;
}

export default function BioCard({bio}: Props) {
    return (
        <div> 
        </div>
    );
}